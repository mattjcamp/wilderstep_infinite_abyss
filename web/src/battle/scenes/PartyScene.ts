// Stubbed for v2's battle-sim port — the v1 PartyScene is not used
// here (v2 has its own React-based party screen). Empty module keeps
// import paths inside `src/battle/` from breaking if anything tries
// to reach for the v1 scene by name.
export {};
