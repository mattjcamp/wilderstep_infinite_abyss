// Deprecated — superseded by the in-map battle flow driven by
// MapEditor → BattleEncounterPicker → MapBattleController + CombatHUD.
// Kept as an empty module so any stale import (none in-tree) doesn't
// break the build.
export {};
