// Deprecated — replaced by the v1-style HUD drawn *inside* the Phaser
// BattleSimScene at /editor/[moduleId]/sim/battle. The React HUD
// approach conflicted with the editor's existing right-rail layout,
// so combat now happens in a self-contained Phaser canvas with no
// React UI around it.
export {};
