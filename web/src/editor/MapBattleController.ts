// Deprecated — superseded by the standalone /editor/[moduleId]/sim/battle
// page, which mounts a self-contained Phaser BattleSimScene against a
// chosen encounter + map. The in-map combat orchestration this file
// used to do is no longer used.
export {};
