// Deprecated — superseded by the in-map battle flow driven by
// MapBattleController + CombatHUD. Kept as an empty module so any
// stale import (none in-tree) doesn't break the build. The catalog
// ref types live in `@/sim/battle/types` now.
export {};
