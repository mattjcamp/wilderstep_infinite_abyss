// Deprecated — the HTML arena layout helper this file used to host
// is replaced by MapBattleController which lays combatants out on the
// editor's real map grid. The shared combat-stat math lives in
// `combatStats.ts` (no Phaser deps), so the kernel and the
// CharacterSheetSim preview agree on damage / AC formulas.
export {};
