"use client";

/**
 * @deprecated Moved to the shared `@/play/LinkPlacard` so the editor's
 * sim mode can reuse it. This re-export remains only so any stale
 * import keeps resolving; prefer importing `LinkPlacard` directly.
 */
export { LinkPlacard, LinkPlacard as PlayLinkPlacard } from "@/play/LinkPlacard";
